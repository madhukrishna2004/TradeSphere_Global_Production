services:
  # 🌐 Frontend (Node.js)
  - type: web
    name: my-web-app
    env: node
    buildCommand: |
      npm install
      npm run build
    startCommand: npm start
    branch: main
    autoDeploy: true
    region: oregon
    envVars:
      - key: NODE_ENV
        value: production

  # ⚙️ Backend (Flask API)
  - type: web
    name: my-backend
    env: python
    runtime: python
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install --force-reinstall numpy==1.26.4 --no-cache-dir
      pip install -r requirements.txt
    startCommand: gunicorn main:app --workers 1 --threads 2 --timeout 120 --bind 0.0.0.0:$PORT
    branch: main
    autoDeploy: true
    region: oregon
    envVars:
      - key: FLASK_ENV
        value: production
